requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.0.0
click>=8.1.0
psutil>=5.9.0
# Optional: enables precise multi-language AST parsing in agent/researcher.py.
# Falls back to regex-based scanning automatically if unavailable — safe to
# skip on platforms where these fail to build (e.g. some Termux setups).
tree-sitter>=0.21.0; platform_system != "Android"
tree-sitter-languages>=1.10.0; platform_system != "Android"
